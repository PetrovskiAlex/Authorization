namespace Authorization
{
    public abstract class Profile
    {
        //RuleForUpdate<Tariff>()
        //    .HaveRole(string)
        //    .HavePermission(string);
        //    .Rule((tariff, context) => tariff.UserId == context.UserId || tariff.CompanyId == context.CompanyId)
        //    .RuleAsync((tariff, context) => accessManager.CanManage(tariff))

        //RuleForInsert<Tariff>()
        //    .HaveRole(string)
        //    .HavePermission(string);

        //RuleForRead<Tariff>()
        //    .HaveRole(string)
        //    .HavePermission(string);
        //    .Rule((tariff, context) => tariff.UserId == context.UserId || tariff.CompanyId == context.CompanyId)
        //    .RuleAsync((tariff, context) => accessManager.CanManage(tariff)
    }
}